ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.example.ApplicationKt.module ]
    }
    database {
            DatabaseUrl="jdbc:postgresql://localhost:5432/ktor_word"
            DatabaseDriver="org.postgresql.Driver"
            DatabaseUser="postgres"
            DatabasePassword="Postgre0819NRyo!"
        }
}

